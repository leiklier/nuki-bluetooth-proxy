# Test/typecheck environment, pinned so CI matches a known-good combination.
# pytest-homeassistant-custom-component pins the matching homeassistant version;
# the bluetooth stack below is required by HA's bluetooth/usb components but not
# installed as part of the homeassistant package itself. Keep the bluetooth pins
# in sync with the bluetooth/usb component manifests of the pinned HA release
# (mismatched habluetooth versions break the mocked scanner teardown in tests).
pytest-homeassistant-custom-component==0.13.345
PyNaCl==1.6.2
bleak==3.0.2
bleak-retry-connector==4.6.1
bluetooth-adapters==2.4.0
bluetooth-auto-recovery==1.6.4
bluetooth-data-tools==1.29.18
dbus-fast==5.0.22
habluetooth==6.26.2
aiousbwatcher==1.1.2
serialx==1.8.2
aioesphomeapi==45.5.2
ibeacon-ble==1.2.0
